# Disable Nixpacks completely - use Dockerfile instead
[build]
nixpacks = false

# If Nixpacks is still used (fallback), force Node 20
[variables]
NODE_VERSION = "20.9.0"

[phases.setup]
nixPacksImage = "node:20-alpine"